#ifndef TESSERA_ASSETS_H
#define TESSERA_ASSETS_H
#include <stdint.h>
/* Painted stage: flash-resident, copied by DMA while geometry is prepared. */
extern const uint16_t stage_day[320*240],stage_night[320*240];
#endif
